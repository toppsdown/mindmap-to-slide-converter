%section.main-section
  %section
    .header=slide.header
    .content.horizontal
      - slide.contents.each do |content|
        %div
          %pre=content
  %section
    .header="#{slide.header}: Teacher Notes"
    .content
      - slide.teacher_notes.each do |tn|
        %pre=tn

